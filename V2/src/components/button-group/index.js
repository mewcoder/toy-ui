import TButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
TButtonGroup.install = function (Vue) {
  Vue.component(TButtonGroup.name, TButtonGroup);
};

export default TButtonGroup;
